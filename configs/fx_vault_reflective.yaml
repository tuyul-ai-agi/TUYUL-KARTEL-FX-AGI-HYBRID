# FX Vault Reflective Config — v5.7.3r++
vault:
  name: FXVault
  integrity_threshold: 0.9
  reflective_mode: adaptive
  journal_hook: true
  bias_sync:
    update_interval: 300
    default_bias: neutral
  connection:
    endpoint: https://api.fxvault.tuyulfx.ai
    token_var: FX_API_TOKEN
